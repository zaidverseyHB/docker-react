sudo: required
services:
  - docker

before_install:
  - docker  build -t zaidverseyhb/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true zaidverseyhb/docker-react npm run test -- --coverage